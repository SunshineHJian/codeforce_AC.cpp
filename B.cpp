#include <cstdio>
#include<iostream>
#include <cmath>/*
#define maxsize 5e9;
using namespace std;
int main()
{
	long long int b;
	cin >> b;
	double c = sqrt(b);
	int count = 0;
	for (int i = 1; i <= c; i++)
	{
		if (b%i == 0)
		{
			if (i == c && b / i == i)//两因子相同
				count++;
			else
				count += 2;
		}
	}
	cout << count << endl;
}*/