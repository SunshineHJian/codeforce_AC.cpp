#include <cstdio>
#include<iostream>
#include <cstring>
#include <vector>
#include <stack>
#include <queue>
#include <algorithm>
#include <cmath>
/*
using namespace std;
int main() {
	long long int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		long long int a, b, k;
		long long int sum;
		cin >> a >> b >> k;
		if (k % 2 == 0) {
			if (a == b)
				sum = 0;
			else {
				sum = (a - b)*k/2;
			}
			cout << sum << endl;
				
		}
		else {
			if (a == b)
				sum = a;
			else
				sum = (a - b)*(k-1)/2+a;
			cout << sum << endl;
		}
	}
}*/