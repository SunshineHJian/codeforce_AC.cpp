#include <cstdio>
#include<iostream>
#include <cstring>
#include <vector>
#include <stack>
#include <queue>
#include <algorithm>
using namespace std;/*
int a[105];
int main() {
	int n,count=0,flag=0;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
		if (a[i] == 1&&i>=3) {
			if (a[i - 1] == 0 && a[i - 2] == 1)
			{
				count++;
				a[i] =0;
			}
		}	
	}
	cout << count << endl;
}*/